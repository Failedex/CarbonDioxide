
(defwindow launcher 
  :geometry (geometry 
              :x "0%"
              :y -50
              :width 0
              :height 0
              :anchor "top center")
              
  :stacking "overlay"
  :exclusive false
  :monitor 0
  :focusable true
  :wm-ignore false
  (barstruct))

(defwidget search [] 
  (overlay
    (box
      :orientation "h"
      :space-evenly false
      :style "
      transition: 500ms; 
      min-height: ${revealdash ? 160 : 40}px;
      min-width: ${revealdash ? 160 : 40}px;"
      (box 
        :width 40)
      (revealer 
        :reveal revealsearch
        :transition "slideright"
        (box 
          :width 950)))
    (box 
      :orientation "h"
      :space-evenly false

      (box
        :space-evenly false
        :orientation "h"
        :class "widget"
        :style "transition: 500ms; 
               ${revealdash ? 'border-radius: 50%; padding: 10px;' : ''}"
        (eventbox
          :onclick "./scripts/ricon.py mode ${revealdash ? 0 : 3} & ${EWW_CMD} update revealdash=${!revealdash} &"
          (transform
            :transform-origin-x "50%"
            :transform-origin-y "50%"
            :rotate {ricon}
            (box 
              :class "ricon ${revealdash ? 'discmode' : 'normal'}"
              :style "
              min-height: ${revealdash ? 130 : 20}px;
              min-width: ${revealdash ? 130 : 20}px;
              background-image: url(\"${revealdash ? pcover : './assets/void.svg'}\");
              "
              (box 
                :halign "center"
                :valign "center"
                :class "cdisc"
                :style "
                min-height: ${revealdash ? 40 : 0}px;
                min-width: ${revealdash ? 40 : 0}px;
                opacity: ${revealdash ? 1 : 0};
                "
                (box 
                  :halign "center"
                  :valign "center"
                  :class "ccdisc"
                  :style "
                  min-height: ${revealdash ? 10 : 0}px;
                  min-width: ${revealdash ? 10 : 0}px;
                  opacity: ${revealdash ? 1 : 0};
                ")))))
        (scroll 
          :hscroll true
          :style "
          transition: 500ms;
          min-width: ${revealsearch ? 160 : 0}px;"
          (input
            :onchange "./scripts/apps.py --query '{}' & ./scripts/ricon.py type \"{}\" &"
            :onaccept "
            ${ arraylength(appsjson.apps) > 0 ? "(cd && ${appsjson.apps[0].desktop}) & ./scripts/apps.py --increase '${appsjson.apps[0].name}' &" : ''}
            ./bin/launcher.sh close &"
            :hexpand true)))
      (scroll 
        :hscroll true 
        :hexpand true 
        (box
          :orientation "h"
          :space-evenly false
          (for a in {appsjson.apps}
            (app :a a)))))))

(defwidget app [a]
  (eventbox
    ; :onclick "(cd && ${a.desktop}) & ./scripts/apps.py --increase '${a.name}' & ./bin/launcher.sh close &"
    (box 
      :orientation "h"
      :space-evenly false
      :spacing 10
      :class "app"
      (image 
        :visible {a.icon != "null"}
        :image-width 20 
        :style "margin: -5px;"
        :path {a.icon != "null" ? a.icon : "./assets/void.svg"})
      (label 
        :text {a.name}))))
      
        
